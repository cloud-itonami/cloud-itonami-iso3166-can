# cloud-itonami-iso3166-can

**`:implemented`** market-entry actor for **CAN**.

Flagship HARD: `ca-presence-missing` · tax HARD: `bn-unverified`

```
clojure -M:dev:test
clojure -M:dev:run
```

AGPL-3.0-or-later.
